# 项目 Spec 总览、全局约定与变更记录

> 版本：v0.4　日期：2026-08-22
> 遵守标准：《SPEC_CODING_STANDARD_v2.md》v2.2
> 上游依据：《1-NL2SQL项目章程_完整版.md》《ADR-0001~0006》

---

## 0. 本文档定位

本文档是项目 Spec 体系的**总索引 + 全局约定**，不承载具体业务规则。具体业务规则按里程碑拆分为独立 Spec 文件（见 §2），各文件遵守本文档的编号、错误码、术语约定。

## 1. 文档与代码的关系（追溯链）

```
章程(PRD) → ADR(人拍板) → Spec(REQ/TC，版本化) → 代码(引用 REQ) → 测试(引用 TC)
     ↑______________________________________________↓
           行为变更先改 Spec → 变更日志 → 重新生成代码
```

- 代码实现处注释/提交信息引用 `// REQ-<模块号>-<流水>`；
- 自动化测试用例名/注释引用 `TC-<模块号>-<流水>-<序号>`；
- 无 REQ 编号的代码视为脱离 Spec（SPEC_CODING_STANDARD §3）。

## 2. 文件组织（里程碑拆分）

| 文件 | 里程碑 | 状态 |
|---|---|---|
| `docs/spec/M1_基础链路.md` | M1 基础链路（单表查询跑通） | ✅ 已签字，已验证 |
| `docs/spec/M2_语义层安全权限审计.md` | M2 语义层+安全+权限+审计 | ✅ 已签字，已验证 |
| `docs/spec/M3_置信度澄清反馈.md` | M3 置信度+澄清+溯源+自我修正+反馈 | ✅ 已签字，已验证 |
| `docs/spec/M4_性能缓存降级.md` | M4 性能+缓存抽象层+降级+会话写穿透 | 起草中 v0.1 |
| `docs/spec/M5_数据治理.md` | M5 Schema 变更检测+数据质量+指标口径管理 | 待启动 |
| `docs/spec/M6_前端观测.md` | M6 前端+LangFuse+成本监控+异常处理 | 待启动 |
| `docs/spec/M7_持续回归.md` | M7 CI/CD+150 用例回归+准确率门禁 | 待启动 |

> 注：M2 Spec 文件名最初为 `M2_语义层安全权限.md`，实现时扩展了审计功能，更名为 `M2_语义层安全权限审计.md`。

## 3. 编号约定

- **REQ 编号**：`REQ-<模块号>-<流水>`，模块号取标准 §2 编号（1=业务目标，2=架构，3=数据模型，4=接口，5=核心逻辑，6=边界/验收）。
- **TC 编号**：`TC-<模块号>-<流水>-<序号>`，回链其验证的 REQ。
- **全局唯一性**：`<流水>` 在项目内**跨里程碑全局递增、永不重复**，由下表统一分配；已分配编号不得回收。

### REQ 编号分配表

| 编号 | 所属里程碑 | 对象 |
|---|---|---|
| REQ-101 | M1 | 系统能力与范围 |
| REQ-201 | M1 | 单服务架构与依赖 |
| REQ-301 | M1 | orders 表数据模型 |
| REQ-401 | M1 | 查询接口 |
| REQ-501 | M1 | SQL 生成 |
| REQ-502 | M1 | SQL 校验（安全红线） |
| REQ-503 | M1 | SQL 执行 |
| REQ-504 | M1 | 结果格式化 |
| REQ-505 | M1 | Prompt 注入防御 |
| REQ-601 | M1 | 边界与验收标准 |
| REQ-302 | M2 | users 表数据模型 |
| REQ-303 | M2 | metric_definitions 表 |
| REQ-304 | M2 | metric_lineage 表 |
| REQ-305 | M2 | business_glossary 表 |
| REQ-306 | M2 | audit_logs 表 |
| REQ-402 | M2 | 登录接口 |
| REQ-403 | M2 | 查询接口 M2 升级（鉴权+权限注入+审计） |
| REQ-506 | M2 | 指标语义层匹配 |
| REQ-507 | M2 | 权限注入 |
| REQ-508 | M2 | 审计日志 |
| REQ-509 | M2 | SQL 校验 AST 升级 |
| REQ-510 | M2 | 业务术语映射 |
| REQ-307 | M3 | feedback_queue 表 |
| REQ-404 | M3 | 查询接口 M3 升级（意图识别+置信度+溯源） |
| REQ-405 | M3 | 反馈提交接口 |
| REQ-511 | M3 | 意图识别 + 置信度评估 |
| REQ-512 | M3 | 反问澄清 |
| REQ-513 | M3 | 自我修正 |
| REQ-514 | M3 | 数据溯源 |
| REQ-515 | M3 | 用户反馈 |
| REQ-308 | M4 | session_records 表 |
| REQ-406 | M4 | 查询接口 M4 升级（缓存命中标记） |
| REQ-516 | M4 | 语义缓存 |
| REQ-517 | M4 | 权限缓存 |
| REQ-518 | M4 | 降级链 |
| REQ-519 | M4 | 会话写穿透 |
| REQ-520 | M4 | LIMIT 注入 |
| REQ-521 | M4 | 缓存监控埋点 |
| … | M5 起 | 后续按全局递增继续分配 |

## 4. 全局错误码

| 错误码 | 含义 | 启用里程碑 |
|---|---|---|
| 2000 | 成功 | M1 |
| 4000 | 参数校验失败 | M1 |
| 4001 | 意图识别非问数 | M3 |
| 4002 | 置信度低，需澄清 | M3 |
| 4003 | SQL 安全拦截 | M1 |
| 4004 | 权限不足 | M2 |
| 401 | 未授权 | M2 |
| 40101 | 用户名或密码错误 | M2 |
| 4290 | 并发超限 | M1 |
| 5000 | 系统内部错误（兜底） | M1 |
| 5001 | 模型 API 超时 | M1 |
| 5002 | 数据库查询超时 | M1 |
| 5003 | 上游返回格式异常 | M1 |

> 4290、5003 为 M1 扩展（登记于 ADR-0001 §附）；401、40101 为 M2 扩展（ADR-0003 A1）。

## 5. 术语

沿用 SPEC_CODING_STANDARD_v2 附录 B，另补：

| 术语 | 定义 |
|---|---|
| 问数 | 以自然语言提问并获取数据结果的意图 |
| 反问澄清 | 置信度不足时，系统向用户返回澄清问题以确认意图，而非直接执行 |
| 中置信 | 0.60 ≤ 置信度 < 0.85，执行额外语义自检后放行或转反问（见 ADR D-02/D-05） |
| 语义缓存 | 以 question+role 为 key 缓存查询结果，命中直接返回（M4） |

## 6. 全局变更记录

| 日期 | 变更人 | 变更 REQ | 变更说明 | 影响范围 |
|---|---|---|---|---|
| 2026-08-18 | 用户 | — | 建立 Spec 体系：00 总览 + M1 v0.1；扩展错误码 4290/5003 | 全项目 |
| 2026-08-18 | 用户 | — | M1 默认值 A~E 类确认（见 ADR-0002） | M1 |
| 2026-08-22 | 用户 | REQ-302~510 | M2 实现：语义层+RBAC+审计+AST 校验 | M2 |
| 2026-08-22 | 用户 | REQ-307/404/405/511~515 | M3 实现：意图识别+置信度+反问+溯源+自我修正+反馈 | M3 |
| 2026-08-22 | 用户 | REQ-308/406/516~521 | M4 Spec v0.1 起草：缓存抽象层+降级+会话写穿透 | M4 |

> **人类评审门（硬门槛）**：每份里程碑 Spec 须经责任人签字确认后方可作为批量生成依据（SPEC_CODING_STANDARD §5）。签字记录见各里程碑 Spec 的「变更记录」节。

# Spec Coding 标准 v2.2

> **定位声明**：本文档是「如何写好一份 Spec」的**写作标准**，不含任何具体项目的业务需求。它作为项目约束被引入，用于指导 AI 与开发者产出合格的 Spec；具体项目的 Spec 是另一份独立文档，需遵守本标准。

> **术语约定**：本文档规范的是**产物「Spec」（文档）**；「Spec Coding」指产出该文档的过程。下文单独出现"Spec"均指产物文档。

---

## 0. 本文档解决什么问题

Spec Coding 不是"随便写一份文档"，而是一份**可直接作为 AI 代码生成输入、具备强约束力、无歧义的正式规约**。

- **目标**：让代码实现、AI 生成、评审拥有统一基准（消除歧义）。
- **工作流**：见 §5（原型探索结束后，所有正式工程代码严格对齐 Spec）。

---

## 1. 三条核心执行标准（判定一份文档是否算 Spec）

这三条是全局标准：**六大模块中的每一项产出，都必须同时满足三原则**，缺一即不合格。

### 标准 1：无歧义原则

禁止主观形容词：`尽量`、`合适`、`优化`、`尽可能`、`妥善`、`合理`。

- 所有逻辑量化，有明确分支；任何人阅读不会产生两种理解。

> ❌ 反面：系统需要妥善处理超长上下文
> ✅ 规范：上下文总 Token 超过 7200 时，执行摘要压缩；压缩后最少保留「当前用户提问 + 上一轮返回结果」。

- 被禁词对照示范：`尽量优化`→`在 X 条件下执行 Y`；`合适的重试次数`→`重试 3 次`；`合理的超时`→`超时 ≤ 30s`；`尽可能复用`→`复用规则 A`。

### 标准 2：实现中立原则（含边界说明）

Spec 只定义"做什么"——行为、输入输出、约束；不强制"怎么写"。

- 不限定特定函数、特定循环写法、具体框架选型。
- 同一份 Spec 可由 Python 或 Java 实现，Spec 不变。

> **边界说明（关键）**：以下内容属于 **Spec 本身、必须写死**，不属于"实现自由"的范畴：
> - 数据模型：字段名、类型、默认值、生命周期（见 §2.3）；
> - 接口契约：路径、参数、返回结构、错误码（见 §2.4）；
> - 线上格式：流式输出格式、缓存结构、持久化字段（与 §2.4「返回结构」同一件事，以 §2.4 为准，此处不再重复定义）。
>
> 而以下内容**留给实现者**：函数命名、循环/递归写法、框架与库选型、类/模块的内部组织。
>
> **判定口诀**：凡是"两个实现者写出来会不一致、且线上会对不齐"的东西，都写进 Spec；只有"换了写法不影响对外行为"的东西才留给实现者。

### 标准 3：可验证、可自动化测试

每条业务规则，理论上都能写出测试用例；如果一条需求无法构造 case 验证，说明 Spec 写得模糊，不合格。

---

## 2. 六大标准模块（每项均需满足三原则）

每个模块末尾附「完成定义（DoD）」——写满哪些项才算该模块合格。DoD 必须是**可判定条件**，本身也不得使用主观形容词。

### 2.1 业务目标 & 非功能性需求（顶层约束）

**必须写清楚：**

| 类别 | 内容 |
|------|------|
| 系统能力 | 核心能力、使用人群 |
| 性能阈值 | 单次调用超时、并发上限、会话存活时间 |
| 安全红线 | 禁止高危 SQL、Prompt 注入防御、数据访问权限 |
| 可观测 | 日志字段、指标（token 消耗、失败率、重试次数） |

**NFR 强制量化格式**：每条非功能需求必须写成 `指标 + 数字 + 单位 + 测量方式`，否则按"模糊"处理。

> ✅ 规范：单次 LLM 调用超时 ≤ 30s（自网关发出首字节至收到末字节计）。
> ❌ 反面：LLM 调用超时要快。

**安全红线同样必须量化**（本项为**全级别必含**，最小可用 Spec 亦不可省）：不能只列"禁止"二字，必须落到**可验证的拦截规则**——如 Prompt 注入防御需给出可测试的拦截触发条件（例：`message` 含 `忽略以上指令` 或 `system` 覆盖类指令时，拒绝并返回 `40002`）。

**DoD（完成定义）**：每个 NFR 条目都含数字与测量方式；每条安全红线都能写出一个可断言的拦截触发条件；每个可观测指标含采集方式 + 告警阈值（例：5 分钟窗口失败率 > 5% 触发告警）；无任何一句"应尽量/需优化"类表述。

### 2.2 系统架构 & 模块划分

- 模块职责、模块间依赖关系（画出依赖方向）；
- 数据流走向；
- 外部依赖：LLM 服务、Redis、向量库、数据库（标注版本或能力要求）；
- 部署约束：容器资源限制、环境变量配置规范。

**DoD**：每个模块的职责边界能被一名未参与设计者独立复述、且不与其他模块重叠；依赖关系无环或有明确分层。

### 2.3 数据模型规约（实体、状态）

Agent 项目重点：对话状态、会话元数据、元数据表结构、缓存结构。

- 每个字段明确：**字段名、类型、是否允许空值、默认值、生命周期**；
- 枚举状态完整列出，含状态转移条件；
- **不允许实现阶段自由新增隐含字段**。

**DoD**：任何一个字段，不读代码就能确定其类型与默认值；状态机的每个转移都有触发条件。

### 2.4 接口契约（同步/流式接口）

每个接口固定包含五项：

1. 请求路径、请求方式；
2. 请求头约束；
3. 请求参数（必填/选填、格式、取值范围）；
4. 正常返回结构；
5. **全部异常场景：错误码、错误信息、降级策略**。

**Agent 项目重点：流式输出格式、中断恢复参数规范。**

> **中断恢复参数正例**（Agent 流式接口最易遗漏项）：
> - `resume_id`：断点续传令牌（string，选填），服务端据此从指定偏移继续输出；
> - 中断重连约定：客户端携带 `resume_id` 重连，服务端返回「已推送字节数 + 剩余内容」；`resume_id` 超时（>300s）失效。

**兜底默认（必写）**：由于上游/运行时存在无法预先穷举的失败，除已列异常外，必须声明一条**兜底默认**——未穷举的异常统一返回 `500` + 通用话术 + 记录 `error` 级日志；该默认写入 Spec，避免实现者自由发挥。

**DoD**：异常场景覆盖"参数非法 / 鉴权失败 / 上游超时 / 上游返回格式异常 / 并发超限"至少五类，每个异常都有明确错误码与降级动作；已声明兜底默认；流式接口含中断恢复参数约定。

### 2.5 核心业务逻辑规则（Agent 项目重中之重）

必须**可复现、可测试**，不得模糊。典型条目：

- 元数据检索匹配逻辑；
- LLM 生成 SQL 后的校验规则；
- 多轮对话上下文压缩触发条件；
- 工具调用失败后的重试策略、最大重试次数；
- 用户意图模糊时的交互策略。

> ✅ 规范：当对话 Token > 7200 触发摘要压缩，保留最近两轮原始问答。
> ❌ 反面：尽量优化上下文。

**DoD**：每条规则都能写成 `Given/When/Then` 用例（见 §4）。

### 2.6 边界 Case、兜底策略、测试验收标准

- 非法输入如何响应；
- LLM 返回格式异常如何解析与重试；
- 验收成功的判定标准。

> Spec 自带验收标尺，写完就知道做到什么程度算完工。

**DoD**：验收 Case 清单非空，且每条符合 §4 的可执行格式。

---

## 3. 需求编号与追溯

**编号规则（两类编号，勿混用）：**

| 编号 | 对象 | 格式 |
|------|------|------|
| `REQ` | 需求（接口、业务规则、异常） | `REQ-<模块号>-<流水>`，模块号取 §2 编号（1=业务目标，2=架构，3=数据模型，4=接口，5=核心逻辑，6=边界/验收） |
| `TC` | 验收 Case | `TC-<模块号>-<流水>`，并回链其验证的 `REQ` |

- 例：§2.4 接口用 `REQ-401`、`REQ-402`…；§2.5 业务规则用 `REQ-501`…；验证 `REQ-501` 的用例用 `TC-501-01`、`TC-501-02`…
- 生成代码时，**在对应实现处注释/提交信息中引用编号**（例：`// REQ-401`）。
- 评审时以"每行代码能否追溯到某条 REQ"为标准；**无编号代码视为脱离 Spec**。

> 作用：把"是否对齐 Spec"从主观判断变成可机器核查。

---

## 4. 验收 Case 的可执行格式

验收 Case 统一采用 `Given / When / Then` 结构，关键输入输出固化为测试 fixture：

```
TC-501-01（验证 REQ-501 超长上下文压缩）
Given: 上下文 Token 计数 = 7300
When:  下一轮用户提问到达
Then:  触发摘要压缩，压缩后保留「当前提问 + 上一轮返回结果」
```

- 所有 `Then` 必须是可断言的布尔条件或精确值，禁止"表现良好"类表述。
- 每条 `REQ` 至少对应一条验收 Case（一条 REQ 可有多条 TC）；无法构造 Case 的 REQ 视为不合格，回炉重写。
- 自动化测试的用例名/注释须引用 `TC-xxx`，使 `REQ → TC → 测试代码` 三环闭合。

---

## 5. 版本与变更控制

- Spec 带版本号（`v1.0`），变更走修订流程并记录变更日志。
- **禁止代码与 Spec 各自漂移**：任何行为变更必须先改 Spec，再改代码。
- 变更日志最小字段：`日期 / 变更人 / 变更 REQ / 变更说明 / 影响范围`。
- **人类评审门（硬门槛）**：正式代码生成前，Spec 须经责任人签字确认（记录于变更日志）；未经确认的 Spec 不得作为批量生成依据。
- 工作流不是单向的，而是带回流环的闭环：

```
Vibe 探索（不写正式 Spec，允许临时方案/硬编码）
   → 方案收敛，起草完整 Spec
   → 正式工程代码严格对齐 Spec
   → 评审 / 测试失败 → 修订 Spec → 重新生成代码
```

---

## 6. 范围分级（防止 Spec 自我膨胀）

| 级别 | 必含模块 | 适用场景 |
|------|---------|---------|
| **最小可用 Spec** | 2.3 数据模型 + 2.4 接口 + 2.5 核心逻辑 + 2.6 异常/验收 | 原型转正式的最小门槛 |
| **完整 Spec** | 六大模块全含 | 对外交付、多人协作、长期维护 |

- 每份 Spec **必须附带「非目标 / 不做」清单**，明确系统不做什么，防止实现过程中需求无限膨胀。
- 小项目写"最小可用 Spec"即达标，不允许因写不动而干脆不写。
- **无论哪个级别，安全红线（§2.1）均为必含项**：最小可用 Spec 可省 2.1 其余项（性能/可观测）与 2.2 架构，但安全红线不可省。

---

## 7. Spec 交付自检清单

> 本清单是 §2 各模块 DoD 的汇总镜像，**修改以 §2 为准**。标注〔完整〕的项，最小可用 Spec 可跳过；标注〔全级别〕的项，任何级别都必须通过。

提交前逐项打勾，任一必含项未勾即为不合格，回炉：

**三原则（全级别）**
- [ ] 全文无主观形容词（`尽量`/`合适`/`优化`/`尽可能`/`妥善`/`合理`）
- [ ] 数据模型、接口、线上格式均已写死；仅函数命名、循环写法、框架选型留给实现者
- [ ] 每条业务规则都能写出 `Given/When/Then` 用例

**六模块 DoD**
- [ ] 每条安全红线含可断言的拦截触发条件〔全级别〕
- [ ] 每个 NFR 含 `指标 + 数字 + 单位 + 测量方式`〔完整〕
- [ ] 每个可观测指标含采集方式 + 告警阈值〔完整〕
- [ ] 每个模块职责边界可独立复述、且不与其他模块重叠〔完整〕
- [ ] 每个字段：名称 / 类型 / 可空 / 默认值 / 生命周期 齐全〔全级别〕
- [ ] 状态机每个状态转移有触发条件〔全级别〕
- [ ] 接口五项齐全（路径 / 头 / 参数 / 返回 / 异常）〔全级别〕
- [ ] 异常场景覆盖「参数非法 / 鉴权失败 / 上游超时 / 上游返回格式异常 / 并发超限」至少五类〔全级别〕
- [ ] 已声明兜底默认（未穷举异常 → 500 + 通用话术 + error 日志）〔全级别〕
- [ ] 流式接口含中断恢复参数约定〔全级别〕

**机制（全级别）**
- [ ] 每条需求有 `REQ` 编号，每条验收 Case 有 `TC` 编号并回链 `REQ`
- [ ] 自动化测试用例名/注释引用 `TC-xxx`
- [ ] 版本号 + 变更记录已写（含 `日期/变更人/变更 REQ/变更说明/影响范围`）
- [ ] 附「非目标 / 不做」清单
- [ ] Spec 经责任人签字确认后方可作为批量生成依据

---

## 8. 落地引入方式（给 AI 与协作的约定）

- **存放位置**：Spec 独立成文件（建议 `docs/spec/<feature>.md`），与代码分离。
- **AI 引入**：将 Spec 路径/内容注入项目级 `CLAUDE.md` 或 `AGENTS.md`，作为代码生成上下文。
- **拆分建议**：大型 Spec 按六大模块拆为独立文件，通过 `REQ-xxx` 编号互相引用；文件命名建议 `docs/spec/<序号>_<模块>.md`，如 `01_goals.md`、`04_interface.md`。
- **引用方式**：AI 生成代码时，以 `REQ-xxx` 回链到 Spec 具体条目，而非泛泛引用整份文档。

---

## 9. Spec 与 PRD、架构文档的区别

| 文档 | 面向 | 回答的问题 |
|------|------|-----------|
| PRD | 产品 | 我们要做什么功能、给谁用 |
| 架构文档 | 高层结构 | 系统怎么分层、组件怎么连 |
| **Spec** | 实现 | **每一种输入，系统精确输出什么、异常如何处理** |

一句话：PRD 讲"做什么"，Spec 讲"每种输入输出什么、异常怎么办"，粒度下沉到接口、分支逻辑、异常分支，直接作为代码生产依据。

---

## 附录 A：填好的完整正例（示范"合格"长什么样）

### A.1 示例 Spec 片段：对话补全接口

**REQ-401 对话补全接口**

1. **路径/方式**：`POST /api/chat/complete`
2. **请求头**：`Authorization: Bearer <token>`；`Content-Type: application/json`
3. **请求参数**：

| 参数 | 类型 | 必填 | 格式/范围 |
|------|------|------|-----------|
| `session_id` | string | 是 | UUID v4 |
| `message` | string | 是 | 1–4000 字符，UTF-8 |
| `stream` | boolean | 否 | 默认 `false` |

4. **正常返回**：`{ "code": 0, "data": { "reply": "...", "session_id": "..." } }`
5. **异常场景（覆盖四类）**：

| 错误码 | 场景 | 降级策略 |
|--------|------|---------|
| `40001` | `message` 为空或超长（参数非法） | 返回错误，不调用 LLM |
| `40101` | token 缺失/过期 | 返回 401 |
| `50301` | LLM 上游超时（>30s） | 返回 503；重试 1 次后仍失败则返回固定话术「系统繁忙，请稍后重试」 |
| `50201` | 上游返回格式异常（非 JSON 或缺 `reply` 字段） | 返回 502；重试 1 次后仍失败则返回固定话术「服务暂时不可用，请稍后重试」 |
| `42901` | 并发超限（同会话并发请求 > 3） | 返回 429，提示「请求过于频繁，请稍后再试」 |

**REQ-402 中断恢复参数**

- `resume_id`：断点续传令牌（string，选填）；客户端重连时携带，服务端从指定偏移继续推送。
- 中断重连约定：服务端返回「已推送字节数 + 剩余内容」；`resume_id` 超时（>300s）失效。

### A.2 业务规则 + 验收 Case 示例

**REQ-501 超长上下文压缩**

> 当对话 Token > 7200 时执行摘要压缩；压缩后保留「当前提问 + 上一轮返回结果」。

**TC-501-01（验证 REQ-501）**：

```
Given: 上下文 Token 计数 = 7300
When:  下一轮用户提问到达
Then:  触发摘要压缩，压缩后保留「当前提问 + 上一轮返回结果」
```

### A.3 数据模型示例：会话元数据（节选）

| 字段 | 类型 | 可空 | 默认值 | 生命周期 |
|------|------|------|--------|---------|
| `session_id` | string(UUID) | 否 | — | 创建即生成，销毁即删除 |
| `status` | enum | 否 | `active` | `active → compressing → active` |
| `token_count` | int | 否 | 0 | 每轮累加，压缩后重置 |

### A.4 完整「最小可用 Spec」骨架（覆盖 §6 四个模块）

```
# 某功能最小可用 Spec v1.0
## 0. 非目标（不做）清单
   - 不做多租户；不做历史会话导出
## 1. 数据模型（§2.3）
   1.1 会话元数据：字段表（名称/类型/可空/默认值/生命周期）
   1.2 状态机：状态列表 + 每个转移的触发条件
## 2. 接口（§2.4）
   2.1 REQ-401 对话补全接口（五项齐全 + 四类异常）
   2.2 REQ-402 中断恢复参数
## 3. 核心逻辑（§2.5）
   3.1 REQ-501 超长上下文压缩（量化触发条件）
   3.2 REQ-502 工具调用失败重试策略（最大次数 + 退避）
## 4. 异常与验收（§2.6）
   4.1 异常兜底清单
   4.2 TC-501-01 …（每 REQ ≥ 1 条 Given/When/Then）
## 5. 变更记录（§5）
```

---

## 附录 B：术语表

| 术语 | 定义 |
|------|------|
| 会话（Session） | 一次完整多轮对话的持久上下文 |
| 对话状态 | 当前轮对话的瞬时处理状态（如等待工具返回） |
| 元数据 | 本语境下指业务表/字段的**语义描述**，用于检索匹配（区别于描述系统自身的"会话元数据"） |
| 摘要压缩 | 将超长历史上下文压缩为摘要以减少 Token 的行为 |
| 降级策略 | 主路径失败时的备选响应方式 |

---

## 附录 C：变更记录

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| v1.0 | — | 初版：六大模块 + 三条原则，未分离"标准/实例"，缺追溯、版本、分级、落地机制 |
| v2.0 | — | 重写：定位声明前置；实现中立补边界说明；新增需求编号、版本控制、范围分级、落地引入；补附录 A 正例与术语表 |
| v2.1 | 2026-08-18 | 修编号体系（REQ/TC 分离）；补交付自检清单；修附录正例（四类异常齐全、固定话术具体化、中断恢复示例）；DoD 去主观词；补安全红线可测化、变更日志字段、最小可用 Spec 骨架 |
| v2.2 | 2026-08-18 | 安全红线设为全级别必含；自检清单标注适用级别并声明为 DoD 镜像；接口补兜底默认；异常清单扩为五类（补鉴权失败）；可观测补 DoD；补人类评审门、测试绑定 TC、文件命名约定、被禁词对照示范 |

---

*本文档为"写 Spec 的标准"，不是具体项目需求。任何具体项目的 Spec 应另起文档并遵守本标准。*

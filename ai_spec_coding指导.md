# AI Spec Coding 指导

> 本文档回答三个问题：
> 1. 如何让另一个 AI 最大程度复现本项目；
> 2. 下次开新项目要准备什么、怎么开启对话；
> 3. 当前 `CLAUDE.md` 能否复用。
>
> 生成背景：本项目从「一份 PRD + 一份 Spec 写作标准」出发，由 AI 规划 SOP 并执行，完成了 M1~M6 全量交付（后端 + 前端 + 观测 + CI + 部署文档）。本文档沉淀这条路径的可复用方法。

---

## 一、复现当前项目：给哪些文件、做什么操作

### 1.1 核心结论

要复现一个已经做完的 Spec Coding 项目，**不是把代码丢给 AI，而是把「上下文 + 规则」丢给 AI**。代码只是产物，AI 需要的是"为什么这么写、怎么写算对"的规则和决策。

### 1.2 最小复现文件包（按重要性排序）

| 优先级 | 文件 | 作用 | 缺失后果 |
|---|---|---|---|
| ★★★ | `CLAUDE.md` | 项目级约定：Spec Coding 硬约束、错误码、技术栈、JSON 命名、git 约定、codegraph 纪律 | 缺了它，AI 会脱离 Spec 自由发挥 |
| ★★★ | `SPEC_CODING_STANDARD_v2.md` | Spec 写作标准（跨项目通用，讲"怎么写"） | AI 不知道"合格 Spec 长什么样" |
| ★★☆ | `docs/decisions/ADR-*.md`（全部） | 人拍板的决策记录（11+ 份） | AI 遇到歧义会自行决定，偏离你的原始拍板 |
| ★★☆ | `docs/spec/*.md`（全部） | 交付 Spec（每种输入输出什么） | AI 不知道具体需求细节 |
| ★☆☆ | `1-NL2SQL项目章程_完整版.md` | PRD/愿景（讲"做什么"） | 缺背景，但不影响复现已定的 Spec |
| ★☆☆ | `README.md` | 怎么跑起来、账号、测试命令 | 缺了 AI 不知道如何验证 |

> **不必要**：`src/` 代码本身、`frontend/` 前端代码、`git命令说明.md`、`.gitignore`、`deploy/`。这些是产物或工具，AI 需要时能从仓库自己读。

### 1.3 操作步骤（让另一个 AI 复现）

**第 0 步：把仓库交给 AI**

方式二选一：
- 把整个 git 仓库 clone 到 AI 能访问的目录，让它自己读；
- 或把上面 6 类文件的内容直接粘给 AI。

**第 1 步：注入上下文**（复制这段开场白）

```
你在一个已完成的 NL2SQL 项目仓库里。项目采用 Spec Coding 工作方式：
- 必读 CLAUDE.md（项目约定）和 SPEC_CODING_STANDARD_v2.md（Spec 标准）
- docs/decisions/ADR-*.md 是人拍板的决策，遇到歧义以 ADR 为准
- docs/spec/*.md 是交付 Spec，代码对齐它
- 代码实现处注释引用 // REQ-xxx，测试引用 TC-xxx

请先读完上述文档，然后告诉我你对项目的理解，并列出你发现的：
1. 项目架构和模块划分
2. 已完成的里程碑（M1~M6）
3. 剩余的待办（M7）
```

**第 2 步：让 AI 复述**（对齐认知，别急着让它改代码）

让 AI 先输出"它理解的项目"，你核对是否一致。一致后再往下。

**第 3 步：按需下达任务**

- 改代码 → "修改 X，先读 docs/spec/对应里程碑.md，对齐 REQ-xxx"
- 加功能 → "新增功能 Y，先起草 Spec → 我签字 → 再写代码"
- 跑测试 → "mvn test" 或 "mvn test -Dtest=QueryEndToEndIntegrationTest"

### 1.4 复现到什么程度算成功

新 AI 能独立做到：
1. 复述出项目架构 + 里程碑，与你认知一致；
2. `mvn test` 跑绿 43 个单元测试；
3. 能按 Spec Coding 流程（先改 Spec → 签字 → 再改代码）推进一个新需求。

---

## 二、下次开新项目：准备什么、怎么开启

### 2.1 需要准备的文件（两个就够起步）

| 文件 | 是什么 | 标准 |
|---|---|---|
| **PRD / 章程** | 讲"做什么"：目标、用户、架构、技术栈、里程碑 | 见 2.2 |
| **Spec 写作标准** | 讲"怎么写 Spec" | 直接复用 `SPEC_CODING_STANDARD_v2.md`，不用重写 |

> **关键认知**：你不需要一开始就有完整 Spec。本项目就是「PRD + 标准」两件起步，Spec 是 AI 在对话中逐步帮你起草的。所以准备的**最低门槛是"一份合格的 PRD"**。

### 2.2 PRD 的合格标准（决定 AI 能不能干好）

一份能支撑 Spec Coding 的 PRD，至少要写清：

| 要素 | 说明 | 本项目示例 |
|---|---|---|
| **项目目标** | 一句话说清做什么、给谁用 | "自然语言→SQL→执行→可视化，10-20 人企业问数" |
| **核心链路** | 端到端流程分几步 | "意图识别→指标匹配→SQL生成→校验→执行→返回" |
| **技术栈** | 明确的语言/框架/库/版本 | "Java17 + SpringBoot3 + MySQL8 + JSqlParser" |
| **里程碑拆分** | 按周/阶段拆成 M1~Mn | "M1 基础链路 → M2 语义层 → ..." |
| **关键表结构** | 主要数据表字段（可粗） | orders/users/metric_definitions 表字段 |
| **错误码/约定** | 统一响应、错误码表 | `{code,message,data,traceId}` + 2000/4000 系列 |

**不合格 PRD 的特征**（AI 会瞎猜导致返工）：
- 只有愿景没有技术栈（"做个很牛的 AI 系统"）；
- 里程碑含糊（"分几期做"而无具体交付物）；
- 关键数值缺失（缓存多大、超时几秒、重试几次——这些是 Spec 必须量化的）。

### 2.3 怎么开启对话（开场白模板）

```
我要做一个 <项目名>，采用 Spec Coding 工作方式。

我准备了两份文档：
1. <PRD/章程文件名> —— 讲"做什么"
2. SPEC_CODING_STANDARD_v2.md —— 讲"怎么写 Spec"

请先读这两份文档，然后：
1. 复述你对项目的理解
2. 列出你发现的 PRD 里【歧义/矛盾/缺失数值】的地方（这是需要我拍板的决策点）
3. 提出你的 Spec Coding 工作流建议（按里程碑拆分、每步产出什么、我做什么决策）

之后我们按"你列决策点 → 我拍板 → 你起草 Spec → 我签字 → 你写代码"的节奏推进。
```

### 2.4 过程中你的角色（最重要，别当甩手掌柜）

Spec Coding 里**人不是"甲方提需求然后验收"，而是持续做三件事**：

1. **拍板**：AI 会不断列出"PRD 没写清、需要你定"的决策点（阈值、重试次数、缓存 key 等），你逐个拍板，AI 记进 ADR；
2. **签字**：每份 Spec 起草后，你过一遍 DoD 清单，签字后 AI 才写代码；
3. **审查 + git**：AI 写代码后你审查 diff，手动 git 提交（本项目约定 AI 不碰 git）。

---

## 三、当前 CLAUDE.md 能否复用

### 3.1 结论：分层复用，不能整体照搬

`CLAUDE.md` 分两类内容，复用性不同：

| 内容块 | 能否复用 | 处理 |
|---|---|---|
| **Spec Coding 硬约束**（先改 Spec、REQ/TC 回链、ADR 裁决） | ✅ 通用，全项目复用 | 原样保留 |
| **Git 操作约定**（AI 不执行 git、commit message 规范） | ✅ 通用 | 原样保留 |
| **JSON 命名约定**（snake_case ↔ camelCase + @JsonProperty） | ✅ 通用（Java/Spring 项目） | 原样保留 |
| **codegraph 读取纪律**（不重复 Read 等） | ✅ 通用 | 原样保留 |
| **全局错误码速查** | ❌ 项目专属 | 换成新项目的错误码 |
| **技术栈速查** | ❌ 项目专属 | 换成新项目的技术栈 |
| **必读文档表** | ⚠️ 半复用 | 文档名换成新项目的 |

### 3.2 复用模板

新项目直接拷贝这份骨架，替换 `【】` 里的项目专属内容：

```markdown
# CLAUDE.md — 【项目名】

> 本文件是项目级约定，优先级高于 AI 默认行为。每轮会话自动加载。

## 项目定位

【一句话：做什么、给谁用】

## 工作方式（Spec Coding，最高优先级）

本仓库采用 Spec Coding。正式工程代码严格对齐 Spec，禁止脱离 Spec 自由发挥。

**必读文档（按角色）：**

| 文档 | 作用 | 何时读 |
|---|---|---|
| 【PRD 文件名】 | PRD/愿景（讲"做什么"） | 理解需求背景 |
| SPEC_CODING_STANDARD_v2.md | Spec 写作标准（讲"怎么写"） | 起草/审查任何 Spec 前 |
| docs/decisions/ADR-*.md | 人拍板的决策记录 | 遇歧义/冲突时，以 ADR 为准 |
| docs/spec/*.md | 交付 Spec（讲"每种输入输出什么"） | 生成/修改代码前必读对应里程碑 |

## 硬约束（违反即回炉）

1. 行为变更顺序：先改 Spec → 更新变更记录 → 再改代码。
2. 代码回链：实现处注释/提交信息引用 `// REQ-<模块号>-<流水>`。
3. 测试回链：测试用例名/注释引用 `TC-<模块号>-<流水>-<序号>`。
4. 无编号代码视为脱离 Spec。
5. 冲突裁决：ADR > Spec > 章程；ADR 未覆盖的，停下来问，不自行决定。
6. JSON 字段命名（易漏，强制）：对外接口与 LLM 输出字段用 snake_case；Java record/DTO 组件用 camelCase 时必须加 @JsonProperty 显式桥接。

## Git 操作约定

本仓库所有 git 操作由用户手动执行，AI 不执行任何 git 命令。
- 到达提交点时，AI 输出完整 git 命令供用户复制执行；
- commit message：`<类型>: <简述> // REQ-xxx`。

## 全局错误码速查

【换成新项目的错误码表】

## 技术栈速查

【换成新项目的技术栈】

## codegraph（如已索引）

目录已索引。改代码前用 codegraph_explore 查影响范围。
读文件纪律：① codegraph 返回的源码=已 Read，不再重复 Read；② 自己刚写的不回读；③ 查单个符号精确到符号名。
```

### 3.3 一句话总结

> **复用「方法」，替换「内容」。** Spec Coding 的硬约束、git 约定、命名约定、读取纪律是**可移植的方法论**，原样复用；错误码、技术栈、文档名是**项目专属内容**，必须替换。这也是为什么把方法论写进 CLAUDE.md、把具体需求写进 Spec/ADR 的原因——方法可以带走，需求留在项目里。

---

## 附：本项目的完整资产清单（可交付物一览）

| 类别 | 文件 | 作用 |
|---|---|---|
| 方法论 | `CLAUDE.md`、`SPEC_CODING_STANDARD_v2.md` | 可复用的工作方式 |
| 需求 | `1-NL2SQL项目章程_完整版.md` | PRD |
| 决策 | `docs/decisions/ADR-*.md`（11+ 份） | 人拍板记录 |
| Spec | `docs/spec/M1~M7.md`（7 份） | 交付规约 |
| 评测 | `docs/spec/M7_评测用例集.md`（150 条） | 准确率回归 |
| 部署 | `deploy/*`、`docs/部署手册.md` | 上云 |
| 文档 | `README.md`、`git命令说明.md` | 运行与操作 |
| 代码 | `src/`（后端）、`frontend/`（前端） | 产物 |

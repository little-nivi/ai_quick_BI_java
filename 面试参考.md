# 面试参考 —— NL2SQL 问数系统深挖问答

> 用途：个人级企业问数系统（NL2SQL）项目的面试准备。
> 特点：所有回答都基于本项目真实代码、真实踩坑、真实数据，非空泛八股。
> 覆盖：项目总览 / 架构设计 / 技术选型 / 踩坑与解决 / 功能实现 / AI 辅助开发 / 追问反杀。

---

## 一、项目总览类

### Q1：用一句话介绍这个项目

> 一个个人级的企业问数系统：自然语言提问 → 生成只读 SQL → 执行 → 可视化返回，附带企业级可信度保障（RBAC 权限、指标语义层、置信度闭环、审计、缓存、降级、全链路观测）。

**关键钩子**（引导面试官往下问）："它的核心竞争力不是能生成 SQL，而是——**不准时能告诉你它不准，出了问题能追溯定位**。"

### Q2：项目规模和你的角色

- 目标用户 10-20 人（运营/产品/管理层多角色）；
- 后端 Java 单服务（Spring Boot），前端 Vue3，MySQL + Flyway；
- 数据规模：orders 表样例 5000 条，规划 150 万条；
- **我全栈独立完成**：从 PRD 到代码，从后端到前端到部署文档。

### Q3：项目里最有技术含量的三件事

1. **RBAC 权限注入**：不信任模型输出，在 SQL 执行前强制追加 region 过滤，杜绝越权；
2. **缓存抽象层**：CacheService 接口 + Caffeine 实现，预留 Redis 平滑切换；
3. **降级容灾链**：模型超时 → 语义缓存兜底 → 友好提示，多级降级。

---

## 二、架构设计类

### Q4：整体架构是怎么设计的

```
Vue3 前端 → HTTPS+JWT → Spring Boot 单服务
  ├── 意图识别 + 置信度评估（低置信→反问澄清）
  ├── 指标语义层匹配（命中→模板SQL，未命中→NL2SQL）
  ├── SQL 校验（JSqlParser AST 级白名单）
  ├── 权限注入（按 role 追加 region WHERE）
  ├── LIMIT 注入 + 超时控制
  ├── 语义缓存（Caffeine，key 含 role 防串读）
  ├── 只读连接执行（双数据源）
  ├── 自我修正（执行报错→重试≤3次）
  ├── 数据溯源 + 审计日志 + LangFuse 观测
  └── 降级链（超时→缓存→提示）
```

**设计原则**：单服务优先（10-20 人不需要微服务），但内部模块边界清晰、依赖单向无环，为未来拆分留余地。

### Q5：为什么是单服务而不是微服务

**这是有意的务实选择，不是偷懒。**
- 10-20 人、日均 100 次查询，微服务是过度设计；
- 单服务但内部模块化（`query`/`auth`/`cache`/`governance`/`observability` 分层清晰）；
- 用 **CacheService 接口** 而非 Redis 集群，正是"单机务实 + 未来可扩展"的体现——**有边界的架构感**是面试官看重的点。

### Q6：为什么设计双数据源（读写分离）

**安全红线倒逼出来的设计。** 章程要求"MySQL 连接强制只读"（SR-2），但 Flyway 建表是写操作。单数据源 `read-only=true` 会连 Flyway 一起卡死。解法：
- 主数据源（读写）→ 供 Flyway 迁移建表；
- 只读数据源（`readOnly=true`）→ 供 SqlExecutor 查询。

**面试价值**：这不是"为了读写分离而读写分离"，是**安全约束与工程约束冲突时，用架构化解**。而且 NL2SQL 场景下"查询必须走只读连接"是合规底线。

### Q7：核心链路的每个环节，哪一步最容易出错

**权限注入**和**置信度闭环**这两个环节，是"AI 生成 SQL"最容易翻车、也最能体现工程能力的地方：

1. **权限注入**：模型可能生成"全国销售额"的全量 SQL，如果不注入 region 过滤，operator 就能越权看到全量数据。我在执行前**强制**注入，不信任模型；
2. **置信度闭环**：模型可能"自信地生成错误的 SQL"。我让模型自评置信度，低置信不执行、转反问澄清。

---

## 三、技术选型类

### Q8：为什么用 JSqlParser 做 SQL 校验

**从正则到 AST 的演进**，这是本项目一个真实的、能讲的优化点：

- **M1 用正则白名单**（`\b(DROP|DELETE|...)\b`）：简单但会**误杀**——SQL 里字符串字面量含"DELETE"（如 `WHERE note='请 DELETE 此订单'`）会被错判为危险操作；
- **M2 升级到 JSqlParser AST**：解析成语法树，判断根节点是否为 `SELECT`，从语义层面拦截，彻底消除正则误杀。

**面试价值**：体现"知道正则的边界在哪，知道什么时候该上 AST"。

### Q9：为什么用 Caffeine 而不是 Redis

**成本 + 场景驱动的选型，不是拍脑袋：**

| 维度 | Caffeine | Redis |
|---|---|---|
| 延迟 | 纳秒级（本地内存） | 毫秒级（网络） |
| 运维 | 零成本 | 需独立服务 |
| 单机场景 | 完美契合 | 过度设计 |
| 跨实例共享 | 不支持 | 支持 |

**关键**：我做了 `CacheService` 接口抽象，当前用 Caffeine 实现，预留 `RedisCacheServiceImpl`。未来多实例部署时，**只改一个 Bean 的 `@Primary` 注解**就能切换，业务代码零改动。

### Q10：为什么 REST 直连 DashScope/LangFuse，不用官方 SDK

**三个原因**：
1. **SDK 不可用或过重**：LangFuse Java SDK 根本没发布到 Maven Central（我实测验证过）；Spring AI Alibaba 的 Agent/Graph 对"M1 只生成一条 SQL"是杀鸡用牛刀；
2. **REST 直连可控**：OpenAI 兼容端点格式稳定，`RestClient` 就能搞定，还便于加超时、降级、token 统计；
3. **选型显式延后**：把"是否引入重型框架"的决策推迟到确实需要时（ADR D-09 记录了这一点）。

### Q11：怎么控制 LLM 的成本

- **模型分级**：dev 用 qwen-turbo（最便宜）、staging 用 plus、prod 用 max（章程规划）；
- **语义缓存**：重复问题 24h 内直接返回，预估 30% 命中率，省 30% token；
- **token 统计**：接入 qwen 的 `usage` 字段，LangFuse 记录每次调用 token 消耗；
- **降级链**：超时降级到更便宜模型或缓存，避免重试烧钱。

---

## 四、踩坑与解决类（最真实、最有说服力）

> 这部分是面试的"杀手锏"——大厂面试官最爱问"你遇到什么坑怎么解决的"，真实的坑比背八股强百倍。

### Q12：开发中遇到最深的坑是什么

**git 版本错乱导致代码回退，且编译能通过、半天没发现。**

- **现象**：手动 git 操作时，M2/M3 对既有文件的修改丢失了，只剩"新文件 + 旧文件"的混合体，`QueryService` 是 M1 版却引用 M3 的新组件——**编译其实过不了**，但我一开始没跑编译，没发现；
- **根因**：提交时只 `git add` 了新文件，漏了被修改的既有文件（`git status` 没仔细看）；
- **恢复**：用 `git reflog` 找到游离提交 `b9c6d4b`（M2 完整代码）+ `full_local.patch`（M3 补丁），`git checkout <hash> -- <文件>` 逐个恢复；
- **教训 → 落地两条防线**：
  1. **CI**：push 自动跑测试，坏代码立刻红；
  2. **git 手册 + 约定**：AI 不碰 git，提交前必须 `git status` 看清楚。

**面试价值**：这体现了"遇到灾难性问题的冷静排查 + 用工程手段防复发"，比"我写过 XX 功能"更打动面试官。

### Q13：JSON 字段命名踩了什么坑，怎么根治的

**snake_case ↔ camelCase 的映射丢失，踩了三次。**

- **现象**：LLM 返回 `is_query`（snake_case），Java record 组件是 `isQuery`（camelCase），Jackson 默认不自动映射，导致字段**静默为 null**——`is_query=false` 没生效，非问数问题拦不住；
- **踩了三次**：`is_query`、`feedback_type`、`related_tables` 各踩一次；
- **根治**：写进 `CLAUDE.md` 硬约束——对外接口/LLM 字段一律 snake_case，Java 侧 camelCase 必须加 `@JsonProperty` 显式桥接，新增 DTO 前先自查。

**面试价值**：体现"不是修完就算，而是把教训固化成规则，防止团队再犯"——这是 senior 和 junior 的区别。

### Q14：LangFuse 接入踩了什么坑

**三个连环坑，最终用最笨的办法（Python 直连验证）定位。**

1. **SDK 不存在**：Maven Central 搜不到 langfuse-java，决定 REST 直连；
2. **ingestion 格式**：generation 元素缺 `id` + `traceId` 关联，导致 400 被拒（但 HTTP 返回 207，表面"成功"实际没入库）——用 Python 逐步试格式才定位；
3. **timestamp 精度**：Java `Instant.now()` 生成 9 位纳秒小数，LangFuse 只接受毫秒（3 位），`truncatedTo(MILLIS)` 修复；
4. **查询端点废弃**：写入成功了但查不到，最后发现 LangFuse v4 废弃了旧的 `/traces` 端点，要用 `/v2/observations`。

**面试价值**：体现"对接第三方服务时，能通过抓响应体、降级到最小可复现脚本（Python）来定位"的排查能力。

### Q15：Spring 的依赖注入踩过什么坑

**双数据源改造时的两个连环坑：**

1. **Bean 名冲突**：`CacheConfig` 里 `@Bean` 方法名 `semanticCache` 和 `@Component` 类 `SemanticCache` 默认 bean 名撞了，启动报 `overriding disabled`；
2. **Qualifier 不匹配**：`@Qualifier("semanticCache")` 与实际 bean 名 `semanticCacheService` 不符，启动报 `No qualifying bean`。

**根因**：自己手写双数据源/多 bean 配置时，命名不一致。**解法**：统一命名规范（`*Service` 后缀）+ 理解 Spring 的 `@Primary` 与 `@Qualifier` 机制。

### Q16：JdbcTemplate 的 lambda 类型推断坑

`jdbcTemplate.execute()` 有 `ConnectionCallback` 和 `StatementCallback` 两个重载，lambda 参数不标类型时编译器推断成 `Connection`（没有 `setQueryTimeout` 方法），报"找不到符号"。解法是显式标注 `(Statement statement)`。

**面试价值**：体现对 Java 泛型/函数式接口重载的深入理解，不是只会 `@Autowired` 的 CRUD。

---

## 五、功能实现深挖类

### Q17：权限注入是怎么实现的，为什么"不信任模型"

**实现**（`PermissionInjector`）：
- `users.data_scope` 存逗号分隔区域（operator 存 `"华东,华南"`，admin 存 `"all"`）；
- SQL 生成并校验后，`data_scope != "all"` 时**强制**在 WHERE 追加 `AND region IN ('华东','华南')`；
- 区域值经单引号转义防注入。

**为什么"不信任模型"**：NL2SQL 场景下，模型可能生成"全国销售额"的越权 SQL。安全不能依赖模型的"自觉"，必须**在代码层强制执行**。这是章程风险表明确要求的，也是企业级系统的底线思维。

**可量化的验证**：operator 问"全国销售额"，返回 238 万（华东+华南），admin 返回 700 万（全量），隔离生效。

### Q18：意图识别是怎么实现的

**和置信度评估合并成同一次 LLM 调用**（ADR D-17 人拍板），不单独调一次模型。

**实现方式**：让 qwen 在生成 SQL 的同一次调用里，除了返回 `{sql, explanation}`，再返回两个字段：

```
{ "sql": "...", "explanation": "...", "is_query": true, "confidence": 0.9 }
```

- `is_query`：判断用户输入是不是"问数"意图（问数据 vs 闲聊/写代码/问天气）；
- `is_query=false` → 直接返回错误码 4001"意图识别非问数"，**不生成 SQL、不执行**。

**怎么让模型稳定输出 `is_query`**：靠 prompt 约束 + few-shot 示例——系统指令里写明"非数据查询意图时 is_query 填 false"，few-shot 里给了正例（"总销售额"→true）和反例（"今天天气怎么样"→false）。

**为什么和置信度合并**：省一次 LLM 调用。意图识别和置信度评估本质都是"让模型对这次请求做个判断"，拆成两次调用是双倍成本 + 双倍延迟，合并成一次 `{is_query, confidence}` 一举两得。

**踩过的坑（呼应 Q13）**：LLM 返回的是 snake_case 的 `is_query`，Java record 组件是 camelCase 的 `isQuery`，Jackson 不自动映射导致字段静默为 null——`is_query=false` 没生效，非问数问题拦不住。加 `@JsonProperty("is_query")` 修复，并固化进 CLAUDE.md 硬约束。

**可展开的追问点**：意图识别和 SQL 生成"耦合在一次调用"有什么代价？（答：耦合让模型既要判断意图又要生成 SQL，可能互相干扰；但通过 few-shot 分正反例能稳定，且省成本。如果未来意图识别变复杂，可以拆成独立前置调用。）

### Q19：置信度闭环怎么设计，低置信怎么处理

**三级置信度 + 分级动作**（ADR D-02 人拍板）：
- `c ≥ 0.85` 高置信：直接执行；
- `0.60 ≤ c < 0.85` 中置信：额外语义自检后放行，溯源标"中置信"；
- `c < 0.60` 低置信：**不执行**，生成反问澄清问题返回前端。

**设计理念**：对应项目定位"不准时能告诉你它不准"——宁可反问，不冒险执行错误 SQL。

### Q20：指标语义层解决什么问题

**让口径一致，避免"同一个指标大家算出不同数字"。**

- `metric_definitions` 表存指标名、同义词、表达式、模板 SQL、版本、审批状态；
- 命中规则：`metric_name` 或同义词完整子串出现在问题中，多命中取最长；
- 命中 → 套模板 SQL，不命中 → 走 NL2SQL；
- 带版本管理 + 审批流（draft→published→deprecated）+ 血缘通知。

**面试价值**：这是企业 BI 系统的核心痛点——指标口径不一致会导致各部门对不上数。语义层 + 审批 + 血缘是标准解法。

### Q21：自我修正怎么实现，为什么最多 3 次

- SQL 执行报错（语法/字段类）→ 把"原 SQL + 错误信息 + 问题"回传模型重新生成，最多 3 次；
- **为什么 3 次**：防止死循环烧 token——3 次修不好说明不是小错误，直接返回提示让用户换问法，而不是无限重试；
- **只对语法/字段类报错修正**，超时（5002）不修正（超时是性能问题，重试没用）。

### Q22：降级链是怎么设计的

**多级降级，逐层兜底**：
1. 模型 API 超时 → 降级到更便宜模型（qwen-plus）；
2. 仍超时 → 查语义缓存该问题最近成功结果；
3. 缓存也没有 → 返回友好提示"系统繁忙，请稍后重试"。

**LangFuse 不可用** → 降级为不记录 Trace，不影响核心功能。

**设计理念**：每一层降级都有明确的触发条件和兜底，核心链路绝不因为观测/缓存等辅助功能挂掉。

### Q23：数据治理做了什么

- **Schema 变更检测**：定时任务每小时对比 `information_schema` 与存储的 MD5 指纹，变更则告警 + 记录；
- **数据质量监控**：每日检查空值率（>10% 告警）、枚举漂移、数据量波动（>50% 告警）；
- **指标口径审批流**：创建→审批→废弃，admin 权限，越权拦截。

**面试价值**：体现"数据库是活的，系统能自动感知变化"的生产级意识。

---

## 六、AI 辅助开发类（2026 年必问热点）

### Q24：你是怎么用 AI 开发这个项目的

**Spec Coding 工作流**，核心是"人拍板，AI 执行"：

```
PRD/章程 → AI 列决策点 → 人拍板(记 ADR) → AI 起草 Spec → 人签字 → AI 写代码 → 人审查+git
```

**关键不是"让 AI 写代码"，而是**：
1. **先定 Spec**：把"每种输入输出什么、异常怎么办"写死，AI 才不自由发挥；
2. **决策可追溯**：每个歧义点人拍板后记进 ADR，AI 遇冲突以 ADR 为准；
3. **代码回链**：实现处注释 `// REQ-xxx`，测试回链 `TC-xxx`，每行代码能追溯到需求。

### Q25：AI 开发最大的风险是什么，你怎么防

**风险：AI 会"自信地生成错误的代码"，且你很难发现。** 本项目真实发生的案例——git 版本错乱后代码回退到 M1，AI 一度没发现，因为"编译能通过"。

**防线（三道）**：
1. **Spec 先行**：AI 不直接写代码，先起草 Spec 让你签字，把需求歧义消灭在写代码前；
2. **测试兜底**：43 个单元测试 + 1 个集成测试，`mvn test` 立刻暴露破坏；
3. **CI 自动化**：push 自动跑测试，坏代码进不了主干。

**面试价值**：面试官问"AI 会不会写出 bug"，你的答案是"会，所以我用 Spec + 测试 + CI 三道防线把它兜住，而不是相信 AI 不会错"。

### Q26：人和 AI 的分工边界在哪

| 人做（不可替代） | AI 做（提效） |
|---|---|
| 拍板（阈值、选型、边界） | 起草 Spec、写代码、写测试 |
| 签字（Spec 评审） | 列决策点、找歧义 |
| 审查 diff、手动 git | 批量生成、重构、补文档 |

**一句话**：**AI 是执行力，人是判断力。** 本项目从头到尾，所有关键决策（HikariCP 连接数、置信度阈值、缓存 key 含角色、双数据源）都是人拍板，AI 只是把决策落实成代码。

### Q27：这套方法能迁移到别的项目吗

**能，且已经沉淀成方法论。** 本项目产出了：
- `CLAUDE.md`（工作约定，可复用骨架）；
- `SPEC_CODING_STANDARD_v2.md`（Spec 标准，跨项目通用）；
- `ai_spec_coding指导.md`（复现 + 新项目启动 + 复用模板）。

**核心可迁移的是"方法"**：Spec Coding 硬约束、git 约定、JSON 命名约定、codegraph 读取纪律。**替换的是"内容"**：错误码、技术栈、文档名。

---

## 七、追问与反杀类

### Q28：如果面试官问"你的准确率能到多少"

**诚实回答**：代码层面完成，但**准确率调优（M7）是评测工程，需要真实跑 150 条用例 + 花钱调 Few-Shot**，我还没做。当前是"链路完整、功能齐全"，不是"准确率已达标 80%"。

**反杀点**：我可以展开讲——准确率低时怎么排查（Schema 召回失败 → 补注释；SQL 生成错 → 补 Few-Shot；术语没识别 → 补 glossary；指标口径 → 补定义），这比"我准确率 90%"更有说服力，因为**体现你知道怎么调优**。

### Q29：如果面试官问"为什么不直接上现成的 NL2SQL 产品/框架"

**反杀**：现成产品是黑盒，**不可信、不可控、不可追溯**。这个项目的价值恰恰在"企业级可信度保障"——RBAC、指标口径、置信度闭环、审计、血缘，这些是"能回答'为什么这个数字可信'"的工程，现成产品给不了。而且做这个项目的目的是**展示工程能力**，不是真的要替代商业 BI。

### Q30：如果面试官问"你的缓存会不会导致数据不一致"

**回答**：这是 NL2SQL 场景的特殊点——**语义缓存是"问题→结果"的缓存，不是"数据→值"的缓存**。我做了几个关键设计防串读和不一致：
1. **key 含 role**：防止 operator 命中 admin 的越权缓存；
2. **key 含 schema_version**：表结构变了，缓存自动失效；
3. **TTL 24h**：即使有延迟，也是"可能返回稍旧的结果"，而不是"错误的结果"，且审计日志标记了 cache_hit。

**可继续深挖**：缓存一致性策略（写后更新 vs 写后删、竞态条件）——我在权限缓存里选了写后更新（避免删后竞态），这是能展开的点。

### Q31：如果面试官问"你的系统安全性怎么保证的"

**分层安全，逐条可量化**（这是 Spec 里的 SR 红线）：
1. **SQL 注入**：AST 级白名单，只放行 SELECT；
2. **Prompt 注入**：正则拦截"忽略以上指令"类攻击；
3. **越权**：权限注入不信任模型，代码层强制 region 过滤；
4. **数据泄露**：只读连接，杜绝写操作；
5. **密钥泄露**：环境变量注入 + `.gitignore` + 曾建议轮换 key。

**每个都能展开成"拦截触发条件 → 拦截动作 → 测试用例"，这是 Spec Coding 带来的可验证性。**

---

## 附：面试开场 2 分钟自我介绍模板

> "我做了一个个人级的企业问数系统（NL2SQL），自然语言提问自动生成 SQL 并返回可视化结果，目标用户 10-20 人。它的定位不是'能生成 SQL'，而是'每个答案都可信赖，不准时能告诉你它不准，出问题能追溯'。
>
> 技术上，后端是 Spring Boot 单服务，我做了几个企业级设计：RBAC 权限注入（不信任模型、代码层强制过滤）、指标语义层（口径一致 + 审批 + 血缘）、置信度闭环（低置信反问不瞎猜）、缓存抽象层（Caffeine 当下务实、接口预留 Redis）、降级链、LangFuse 全链路观测。
>
> 整个项目我用了 Spec Coding 的方式配合 AI 开发——先定 Spec、人拍板决策、AI 写代码、测试 + CI 兜底，43 个单元测试 + 集成测试全绿。
>
> 我最想讲的是两个真实踩坑：一次 git 版本错乱导致代码回退的排查和恢复，以及权限注入'不信任模型'的设计。"
